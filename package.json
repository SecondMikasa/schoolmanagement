{
  "name": "school-management-api",
  "version": "1.0.0",
  "description": "API for managing school data with location-based sorting",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "pnpx prisma generate",
    "prisma:generate": "pnpx prisma generate",
    "prisma:migrate": "pnpx prisma migrate dev --name init"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1"
  },
  "devDependencies": {
    "@prisma/client": "^6.7.0",
    "nodemon": "^3.0.3",
    "prisma": "^6.7.0"
  }
}
